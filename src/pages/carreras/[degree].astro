---
import Layout from '../../layouts/Layout.astro';

export async function getStaticPaths() {
  // Fetch or import your degree program data
  const degreePrograms = [
    // Your degree program data here
  ];

  return degreePrograms.map((program) => ({
    params: { slug: program.slug },
    props: { program },
  }));
}

const { program } = Astro.props;
---

<Layout title={`${program.name} | Universidad Itecce`} description={program.description}>
  <section class="degree-program-details">
    <h1>{program.name}</h1>
    <!-- Add more details about the degree program -->
  </section>
</Layout>
